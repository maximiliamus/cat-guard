# Local Rules

- WSL2 on Windows 11 is used for development.

- Default terminal to run CLI commands is Bash.

- Python with venv is used for development.

- Always reply in English for all chat messages, code, filenames, and documentation. Only switch languages if I explicitly request another language.
